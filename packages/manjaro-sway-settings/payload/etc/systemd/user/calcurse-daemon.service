[Unit]
Description=Calcurse calendar daemon
PartOf=sway-session.target

[Service]
Type=forking
ExecStartPre=-rm -f ~/.local/share/calcurse/daemon.lock
ExecStart=/usr/bin/calcurse --daemon
Restart=on-failure
RestartSec=5

[Install]
WantedBy=sway-session.target
