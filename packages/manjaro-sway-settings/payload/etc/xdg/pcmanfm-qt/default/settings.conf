[System]
# pcmanfm-qt defaults to Terminal=xterm, which we don't ship. Seed the
# system-wide profile instead of etc/skel so existing users pick it up too:
# pcmanfm-qt falls back to this file whenever the user has no profile of their
# own, and writes its own copy on first exit.
Terminal=xdg-terminal-exec
