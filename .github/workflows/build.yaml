name: build
on:
  schedule:
    - cron:  '30 2 * * 0'
  workflow_dispatch:
  repository_dispatch:
    types:
    - iso_profile_update
    - image_profile_update
  push:
    branches:
      - main
    paths:
      - .github/workflows/build.yaml
      - ci/**

concurrency: 
  group: ${{ github.workflow }}
  cancel-in-progress: true

env:
  MIRROR: https://opencolo.mm.fcix.net/manjaro

jobs:
  prepare-release:
    runs-on: ubuntu-24.04
    steps:
      - id: time
        uses: boredland/get-time-action@dbc808befb89eb33fee64f5f4abce4d2a5fa7cfd # tag=2.0.0
        with:
          format: 'YYYYMMDDHHmm'
      - id: receive
        uses: boredland/kernel-info@95a8330bcb748769fbba1923cf885aaa55098a54 # main
    outputs:
      release_tag: ${{ steps.time.outputs.time }}
      longterm: ${{ steps.receive.outputs.longterm-pkg }}
      stable: ${{ steps.receive.outputs.stable-pkg }}
  build-image:
    runs-on: ubuntu-24.04
    needs: [prepare-release]
    continue-on-error: true
    strategy:
      fail-fast: false
      matrix:
        branch: [unstable]
        device: [rpi4]
    steps:
      - name: import sway key
        run: gpg --keyserver keys.openpgp.org --receive-keys ${{ vars.GPG_KEYID }}
      - name: prepare pacman pkg cache dir
        # Runner-writable so actions/cache can restore here without sudo,
        # and pacman (running as root inside basestrap) can still write.
        run: |
          sudo mkdir -p /var/cache/pacman/pkg
          sudo chmod 0777 /var/cache/pacman/pkg
      - name: cache pacman packages
        uses: actions/cache@v4
        with:
          path: /var/cache/pacman/pkg
          key: pacman-pkg-${{ matrix.branch }}-${{ matrix.device }}-${{ runner.os }}-${{ runner.arch }}-${{ github.run_id }}
          restore-keys: |
            pacman-pkg-${{ matrix.branch }}-${{ matrix.device }}-${{ runner.os }}-${{ runner.arch }}-
      - id: build
        name: arm build
        uses: manjaro-contrib/action-buildarmimg@d18252a49e9dbdece2a41dd58b1d2c7c5d96c8bf # main
        with:
          device: rpi4
          edition: sway
          branch: ${{ matrix.branch }}
          release-tag: ${{ needs.prepare-release.outputs.release_tag }}
          arm-profiles-repo: https://github.com/manjaro-sway/arm-profiles.git
          overlay-repo: https://packages.manjaro-sway.download/${{ matrix.branch }}/aarch64/manjaro-sway.db
      - name: upload image artifact
        uses: actions/upload-artifact@v4
        with:
          name: image-${{ matrix.branch }}-${{ matrix.device }}
          path: ${{ steps.build.outputs.file-path }}
          retention-days: 1
          if-no-files-found: error
      - name: make pacman cache readable
        # Save step needs to tar root-owned files; ensure permissions allow.
        if: always()
        run: sudo chmod -R a+rX /var/cache/pacman/pkg 2>/dev/null || true
  build-iso:
    runs-on: ubuntu-24.04
    needs: [prepare-release]
    strategy:
      fail-fast: false
      matrix:
        branch: [unstable]
        include:
          - branch: unstable
            kernel: ${{ needs.prepare-release.outputs.stable }}
    steps:
      - name: import sway key
        run: gpg --keyserver keys.openpgp.org --receive-keys ${{ vars.GPG_KEYID }}
      - name: prepare pacman pkg cache dir
        run: |
          sudo mkdir -p /var/cache/pacman/pkg
          sudo chmod 0777 /var/cache/pacman/pkg
      - name: cache pacman packages
        uses: actions/cache@v4
        with:
          path: /var/cache/pacman/pkg
          key: pacman-pkg-${{ matrix.branch }}-x86_64-${{ runner.os }}-${{ runner.arch }}-${{ github.run_id }}
          restore-keys: |
            pacman-pkg-${{ matrix.branch }}-x86_64-${{ runner.os }}-${{ runner.arch }}-
      - id: build
        name: x86_64 build
        uses: manjaro-contrib/action-buildiso@377cedc76ac456dca7549920eff95ddcaf45c4dc # main
        with:
          iso-profiles-repo: https://github.com/Manjaro-Sway/iso-profiles
          edition: sway
          branch: ${{ matrix.branch }}
          scope: full
          kernel: ${{ matrix.kernel }}
          release-tag: ${{ needs.prepare-release.outputs.release_tag }}
          additional-trusted-gpg: ${{ vars.GPG_KEYID }}
          build-mirror: ${{ env.MIRROR }}
      - name: upload iso artifact
        uses: actions/upload-artifact@v4
        with:
          name: iso-${{ matrix.branch }}
          path: ${{ steps.build.outputs.file-path }}
          retention-days: 1
          if-no-files-found: error
      - name: make pacman cache readable
        if: always()
        run: sudo chmod -R a+rX /var/cache/pacman/pkg 2>/dev/null || true
  test-iso-boot:
    runs-on: ubuntu-24.04
    needs: [build-iso]
    strategy:
      fail-fast: false
      matrix:
        branch: [unstable]
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - name: install qemu + kvm stack
        run: |
          sudo apt-get update
          sudo apt-get install -y qemu-system-x86 ovmf
          # Self-hosted runners sometimes ship without /dev/kvm permissions
          sudo chmod 666 /dev/kvm || true
      - name: download iso
        uses: actions/download-artifact@v4
        with:
          name: iso-${{ matrix.branch }}
          path: artifact
      - name: boot smoke test
        env:
          SERIAL_LOG_OUT: ${{ github.workspace }}/serial.log
        run: |
          chmod +x ci/boot-smoke.sh
          ISO=$(ls artifact/*.iso | head -n1)
          ./ci/boot-smoke.sh "$ISO" x86_64
      - name: upload serial log on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: serial-iso-${{ matrix.branch }}
          path: serial.log
          if-no-files-found: warn
  test-image-boot:
    runs-on: ubuntu-24.04-arm
    needs: [build-image]
    env:
      QEMU_VERSION: '10.1.0'
      QEMU_PREFIX: ${{ github.workspace }}/.qemu
    strategy:
      fail-fast: false
      matrix:
        branch: [unstable]
        device: [rpi4]
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - name: install runtime deps
        # build deps go in unconditionally because configure needs to be able
        # to relink against system libs during install; runtime deps for
        # extract + KVM go here too.
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            mtools fdisk xz-utils zstd \
            build-essential ninja-build pkg-config \
            libglib2.0-dev libpixman-1-dev libslirp-dev \
            python3 python3-venv flex bison
          sudo chmod 666 /dev/kvm || true
      - name: cache qemu ${{ env.QEMU_VERSION }}
        id: cache-qemu
        uses: actions/cache@v4
        with:
          path: ${{ env.QEMU_PREFIX }}
          key: qemu-${{ env.QEMU_VERSION }}-${{ runner.os }}-${{ runner.arch }}-v1
      - name: build qemu (cache miss)
        if: steps.cache-qemu.outputs.cache-hit != 'true'
        run: |
          # Ubuntu 24.04 ships QEMU 8.2.2 which predates -M raspi4b (QEMU 9.0).
          # Source build of just the aarch64-softmmu target lands at ~50 MB.
          curl -fsSL "https://download.qemu.org/qemu-${QEMU_VERSION}.tar.xz" | tar -xJ
          cd "qemu-${QEMU_VERSION}"
          ./configure \
            --prefix="${QEMU_PREFIX}" \
            --target-list=aarch64-softmmu \
            --enable-kvm \
            --enable-slirp \
            --disable-docs \
            --disable-gtk \
            --disable-sdl \
            --disable-vnc \
            --disable-curses \
            --disable-tools
          make -j"$(nproc)"
          make install
      - name: prepend qemu to PATH
        run: echo "${QEMU_PREFIX}/bin" >> $GITHUB_PATH
      - name: download image
        uses: actions/download-artifact@v4
        with:
          name: image-${{ matrix.branch }}-${{ matrix.device }}
          path: artifact
      - name: extract kernel + initramfs
        run: |
          chmod +x ci/extract-rpi-kernel.sh
          IMG=$(ls artifact/*.img* | head -n1)
          ./ci/extract-rpi-kernel.sh "$IMG"
      - name: boot smoke test
        env:
          SERIAL_LOG_OUT: ${{ github.workspace }}/serial.log
        run: |
          chmod +x ci/boot-smoke.sh
          # extract-rpi-kernel.sh decompresses in place; pick the resulting raw img
          IMG=$(ls artifact/*.img | head -n1)
          ./ci/boot-smoke.sh "$IMG" aarch64
      - name: upload serial log on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: serial-image-${{ matrix.branch }}-${{ matrix.device }}
          path: serial.log
          if-no-files-found: warn
  test-iso-install:
    # Level 1 of the install test plan: boot the live ISO and verify the
    # Calamares process actually exec's. Catches installer-package missing,
    # autostart broken, dependency rot. Does *not* drive the wizard UI —
    # that's Phase 2 / issue #982.
    runs-on: ubuntu-24.04
    needs: [build-iso]
    strategy:
      fail-fast: false
      matrix:
        branch: [unstable]
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - name: install qemu + kvm stack
        run: |
          sudo apt-get update
          sudo apt-get install -y qemu-system-x86 ovmf
          sudo chmod 666 /dev/kvm || true
      - name: download iso
        uses: actions/download-artifact@v4
        with:
          name: iso-${{ matrix.branch }}
          path: artifact
      - name: installer launch test
        env:
          SERIAL_LOG_OUT: ${{ github.workspace }}/serial.log
          BOOT_MARKER_REGEX: 'exe="/usr/bin/calamares"'
        run: |
          chmod +x ci/boot-smoke.sh
          ISO=$(ls artifact/*.iso | head -n1)
          # Boot + greetd + sway autostart + calamares exec — give it 12 min.
          ./ci/boot-smoke.sh "$ISO" x86_64 720
      - name: upload serial log on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: serial-install-${{ matrix.branch }}
          path: serial.log
          if-no-files-found: warn
  trigger-mirror:
    runs-on: ubuntu-24.04
    needs: [build-iso, build-image, test-iso-boot, test-iso-install, test-image-boot]
    steps:
      - uses: peter-evans/repository-dispatch@28959ce8df70de7be546dd1250a005dd32156697 # v4
        with:
          token: ${{ secrets.DISPATCH_TOKEN }}
          repository: manjaro-sway/manjaro-sway
          event-type: new_release
  rollback:
    runs-on: ubuntu-24.04
    needs: [build-iso, build-image, prepare-release]
    if: ${{ failure() || cancelled() }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - name: rollback
        run: |
          echo ${{ github.token }} | gh auth login --with-token
          ASSET_COUNT=$(gh release view ${{ needs.prepare-release.outputs.release_tag }} --json "assets" --jq '.assets | length')
          if [ "$ASSET_COUNT" -gt "2" ]; then exit 0;fi
          gh release delete ${{ needs.prepare-release.outputs.release_tag }} -y --repo ${{ github.repository }}
          git push --delete origin ${{ needs.prepare-release.outputs.release_tag }}
