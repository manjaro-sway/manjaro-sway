*.iso
manjaro-tools
pkg-cache
manjaro-full.zip

# build output of the python in scripts/
__pycache__/

# makepkg's working directories, which every packages/<name>/ can grow.
# Anchored to packages/: an unanchored src/ also matches worker/src, which
# is source.
packages/*/pkg/
packages/*/src/
*.pkg.tar.zst
*.pkg.tar.zst.sig

worker/node_modules
worker/.wrangler

# uv's cache, kept in the checkout so a runner can cache one directory
# (UV_CACHE_DIR in mise.toml)
.uv-cache/
